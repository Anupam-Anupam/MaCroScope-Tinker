# Copy to ~/.tinker_env (chmod 600) and fill in real values.
# The Slurm launcher sources this file automatically if present.

export TINKER_API_KEY="tml-..."
export WANDB_API_KEY="..."   # optional; training skips W&B if unset
export HF_TOKEN="hf_..."     # optional; only needed for push_to_hf.py
